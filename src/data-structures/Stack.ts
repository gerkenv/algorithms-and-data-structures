import { Node } from './LinkedList';

/**
 * Stack based on singly linked list
 */
export class StackOnLinkedList {
  private _first: Node;
  /**
   * Create a new stack.
   */
  constructor() {
    this._first = null;
  }

}

