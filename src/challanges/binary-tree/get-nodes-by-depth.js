const node = require('../../dist/data-structures/BinaryTree').Node;


function getNodesByDepth(root) {
    let listHeads =  [];
    listHeads.push(root);

    for(let i = 0; i < listHeads.length; i++) {
        let node = listHeads[i];
        while (node) {
            if (node.left) 
        }
    }
}

addNodeToHeadsArray(listHeads, node) {
    if (listHeads.length == 0) {
        
    }
}

module.exports.getNodesByDepth = getNodesByDepth;